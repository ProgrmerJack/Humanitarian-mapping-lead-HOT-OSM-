# Outputs Directory

```
outputs/
├── impact_tracker.csv          # Session-level stats (currently simulated placeholders)
├── project_32710_summary.csv   # Baseline HOT TM project summary via API
├── validation_log_template.csv # Validation activity log (seeded with example rows)
└── README.md                   # This file
```

Replace the simulated entries with live figures after each mapping/validation session. Keep filenames stable so the after-action note and evidence package remain in sync.
